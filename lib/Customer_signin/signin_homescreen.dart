import 'package:flutter/material.dart';
class SigninHomescreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Welcome to Global Tours & Travels'),
      ),
      body: Center(
        child: Text('Welcome to the Welcome Screen'),
      ),
    );
  }
}